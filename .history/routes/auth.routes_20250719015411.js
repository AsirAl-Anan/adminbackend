import { Router } from "express";

const router = Router()


router.post('/login', (req,res)=>{
try {
    
} catch (error) {
    
}
})
router.post('/logout', (req,res)=>{

})




export default router;